import { Marker } from "react-map-gl";interface Props {  latitude: number;  longitude: number;  color?: string;}const MapMarker = ({ latitude, longitude, color = "red" }: Props) => {  return (    <Marker longitude={longitude} latitude={latitude}>      <div        style={{          backgroundColor: color,          width: 20,          height: 20,          borderRadius: "50%",          border: "2px solid white",          transform: 'translate(-50%, -50%)', // This centers the marker on the coordinates          position: 'relative',          top: '50%',          left: '50%'        }}      />    </Marker>  );};export default MapMarker;
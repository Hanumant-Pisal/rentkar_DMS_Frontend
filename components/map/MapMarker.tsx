import { Marker } from "react-map-gl";interface Props {  latitude: number;  longitude: number;  color?: string;}const MapMarker = ({ latitude, longitude, color = "red" }: Props) => {  return <Marker longitude={longitude} latitude={latitude} anchor="bottom">    <div style={{ backgroundColor: color, width: 20, height: 20, borderRadius: "50%", border: "2px solid white" }} />  </Marker>;};export default MapMarker;
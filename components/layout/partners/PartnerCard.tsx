import { Partner } from "../../types";interface Props {  partner: Partner;  onClick?: () => void;}const PartnerCard = ({ partner, onClick }: Props) => {  return (    <div      onClick={onClick}      className="border p-4 rounded mb-4 shadow hover:shadow-lg cursor-pointer transition"    >      <h3 className="font-bold text-lg">{partner.name}</h3>      <p><strong>Email:</strong> {partner.email}</p>      {partner.vehicleNumber && <p><strong>Vehicle:</strong> {partner.vehicleNumber}</p>}      <p><strong>Status:</strong> {partner.isAvailable ? "Available" : "Unavailable"}</p>    </div>  );};export default PartnerCard;
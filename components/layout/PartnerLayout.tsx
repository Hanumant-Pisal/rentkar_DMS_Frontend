import { ReactNode, useState } from "react";import Navbar from "./Navbar";import { Sidebar } from "./Sidebar";import { cn } from "@/lib/utils";interface PartnerLayoutProps {  children: ReactNode;  className?: string;}const PartnerLayout = ({ children, className }: PartnerLayoutProps) => {  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);  return (    <div className="min-h-screen bg-gradient-to-br from-gray-900 to-blue-900 font-sans antialiased text-white">      <Navbar onMenuClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)} />      <div className="flex h-[calc(100vh-4rem)]">        <div           className={cn(            "fixed inset-y-0 left-0 z-40 w-64 transform bg-gray-900/80 backdrop-blur-md transition-all duration-300 ease-in-out md:relative md:translate-x-0",            {              "translate-x-0 shadow-2xl": isMobileMenuOpen,              "-translate-x-full md:translate-x-0": !isMobileMenuOpen,            }          )}        >          <Sidebar onLinkClick={() => setIsMobileMenuOpen(false)} role="partner" />        </div>        {}        {isMobileMenuOpen && (          <div             className="fixed inset-0 z-30 bg-black/70 backdrop-blur-sm md:hidden"            onClick={() => setIsMobileMenuOpen(false)}          />        )}        <main          className={cn(            "flex-1 overflow-y-auto p-4 transition-all duration-300 scrollbar-thin scrollbar-thumb-gray-700 scrollbar-track-transparent",            className          )}        >          <div className="mx-auto max-w-7xl space-y-8">            {children}          </div>        </main>      </div>    </div>  );};export default PartnerLayout;